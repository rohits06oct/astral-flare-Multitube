{
  "name": "youtubemulti-server",
  "version": "1.0.0",
  "description": "MultiTube Server for running automation tests and serving static content",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "cd automation && npx playwright test tests/articles.spec.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "playwright": "^1.58.2",
    "@playwright/test": "^1.58.2"
  },
  "engines": {
    "node": ">=18"
  }
}